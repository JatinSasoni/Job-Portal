version: "3.8"
services:
  frontend:
    image: jatinsasoni/job-portal-client
    ports:
      - "80:80"
    restart: unless-stopped
  server:
    image: jatinsasoni/job-portal-server
    ports:
      - "8000:8000"
    env_file: ./server/.env
    restart: unless-stopped
